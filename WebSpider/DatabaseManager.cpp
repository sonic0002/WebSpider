#include "DatabaseManager.h"

DatabaseManager::DatabaseManager(void):hostname("localhost"),username("root"),password("pike"),database("webspider")
{
}

DatabaseManager::~DatabaseManager(void)
{
}
